import styled from 'styled-components';
import tw from 'twin.macro';

export const Container = styled.div`

${tw`mx-auto flex border-y-2 border-0 py-10 items-center border-gray-primary border-solid max-w-6xl`}

ul{
    ${tw` mx-auto flex flex-wrap gap-8 `} 
}

`